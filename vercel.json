{
  "framework": null,
  "buildCommand": "cd fuel-frontend && npm install --include=dev && npm run build",
  "outputDirectory": "fuel-frontend/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
